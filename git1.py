# Branches => Branching allows multiple collaborators work on same project
# PR => Pull request => A request to the owner of the github to merge your code in master branch
# To create a branch  => git branch {branch_name}
# git checkout -b {branch name}

# To view the branches => git branch
# to switch between the branches => git switch{branch name}

# Changes Made